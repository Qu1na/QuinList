import type { MemberWorkloadRow } from '@/utils/projectReports'

/** Alias used by TeamMemberModal to avoid circular imports with projectReports. */
export type TeamMemberWorkload = MemberWorkloadRow
